<div class="top-producteur"><h1><%= @network.name %></h1>
<h2>Producteurs dans leurs Coopératives:</h2>
</div>
<div class="container_producteur">

  <div class="show_producteur">
      <% @network_farmers.each do |farmer| %>
      <div class="show_pdt">
        <%= cl_image_tag farmer.photo.first.key%>
        <%= farmer.name %>
        </div>
      <% end %>
  </div>


  <div class="network-photo">
    <%= cl_image_tag @network.photo.last.key%>
  </div>

</div>


<div class="container">
  <div class="row m-5">
    <% @network_farmers.each do |farmer| %>
      <% farmer.ingredients.each do |ingredient| %>
        <div class="col-md-4 mb-3">
          <div class="card">
            <div class="card-body-producteur">
              <h5 class="card-title"><%= ingredient.name %></h5>
                <%= cl_image_tag ingredient.photo.first.key, height: 100, width: 100%>
              <p>Price: <%= ingredient.price %>€</p>
            <%= simple_form_for @cart_item, url: carts_items_path(network_id: farmer.network, ingredient_id: ingredient), html: { class: 'simple-form' } do |f| %>
            <div class="form_product_quantity">
              <%= f.input :quantity, as: :numeric, input_html: { value: 1, min: 1, class: 'form-control', style: "width:50%" }, label: 'Quantity:' %>
              <%= f.submit 'Add to Cart', class: 'btn btn-primary mt-2' %>
            </div>
            <% end %>
          </div>
        </div>
      </div>
    <% end %>
  <% end %>
</div>
</div>
```
